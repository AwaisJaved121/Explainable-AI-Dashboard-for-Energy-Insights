# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ─── Node / React ───
node_modules/
frontend/build/
npm-debug.log*
package-lock.json

# ─── Model artifacts & data ───
# backend/saved_models/ is committed on purpose: the Vercel serverless
# backend loads these .pkl files at runtime and has no build step that
# can regenerate them, so they must ship with the deployed source.
data/
*.csv
*.xlsx

# ─── Evaluation responses ───
backend/questionnaire_responses.json

# ─── Docs / OS ───
Dissertation_Chapters_3-6.md
.DS_Store
Thumbs.db
*.log

# ─── IDE / Editor ───
.vscode/
.idea/
*.swp

# ─── Environment ───
.env
.env.local
