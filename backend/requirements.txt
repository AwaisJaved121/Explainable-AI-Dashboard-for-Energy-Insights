fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
python-multipart==0.0.6
joblib==1.3.2
scikit-learn==1.3.2
pandas==2.1.4
numpy==1.26.3
shap==0.44.0
scipy==1.11.4
openpyxl==3.1.2

# xgboost is only needed to retrain models via train_models.py, not to
# serve backend/main.py — install it separately if you retrain.
# statsmodels and pydantic-settings were listed but are unused by
# both main.py and train_models.py.
# lime was dropped: its matplotlib + scikit-image dependency chain
# pushed the Vercel Python function bundle over the 500MB size limit
# (571.75MB measured). SHAP still covers the /explain endpoints; the
# LIME tab in the frontend falls back to static placeholder data.